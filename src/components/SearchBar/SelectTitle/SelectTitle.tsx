import React from 'react'
import styles from './styles.module.scss'

export const SelectTitle: React.FC = () => {
  return (
    <select placeholder="info" className={styles.selectTitle}>
      info...
    </select>
  )
}
